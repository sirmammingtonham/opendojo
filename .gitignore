# Lua/LuaJIT
*.luac

# C++ / CMake / MSVC build artifacts
build/
build-*/
cmake-build-*/
out/
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
compile_commands.json
install_manifest.txt

# CMake per-user presets (machine-specific paths)
CMakeUserPresets.json

# Visual Studio user state
.vs/
*.user
*.vcxproj.user
*.vcxproj.filters.user
*.suo
*.sdf
*.opensdf

# Native build outputs (in-source builds; out-of-source covered by build/)
*.obj
*.o
*.lib
*.exp
*.ilk
*.pdb
*.idb
*.dll
*.exe

# MSBuild platform/config output dirs (in-source builds only)
[Dd]ebug/
[Rr]elease/
RelWithDebInfo/
MinSizeRel/
x64/
Win32/

# Editor swap
*.swp
*.swo
.vscode/
.idea/

# OS
Thumbs.db
.DS_Store

# OpenDojo artifacts — drills are intentionally per-user, snapshots are dev-only
*.drill
*.bin
snapshots/

# Supabase CLI local state and any local-only project files
supabase/.branches/
supabase/.temp/
supabase/.env
supabase/.env.local

# Private operator runbooks (cloud setup, secrets handling) — never publish
*.private.md

# Rust target dir (research crate)
dll/target/
